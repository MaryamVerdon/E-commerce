{% extends 'base.html.twig' %}

{% block title %}{{ article.libelle }}{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href="/css/article_show.css"/>{% endblock %}

{% block body %}
<div class="container">
  <h3 class="mb-5">Détails</h3>
  <div class="row">
      <div class="col-lg-7">
          <img class="img-fluid" src="{{ article.image }}"/>
      </div>
    <div class="col-lg-5">
      <h2 class="h2-responsive mb-1">{{ article.libelle }}</h2>
      <h3 class="h3-responsive text-right mb-3 mr-3"><strong>{{ article.prixU }} €</strong></h3>
      <div id="description mb-5">
        <h3 class="h3-responsive mb-2">Description</h3>
        <p class="text-muted font-weight-light ml-2">{{ article.description }}</p>
      </div>
      <p>Choissisez la taille</p>
      <div class="d-flex align-items-center flex-column div-tailles mb-5">
        <div id="btns_tailles" class="col-lg-6 col-sm-6 col-6 btn-group btn-group-toggle btns-taille mb-1" data-toggle="buttons">
          {% for q in article.getQuantiteTailles %}
              <label id="btn_taille" class="col-lg-4 col-sm-4 col-4 p-2 btn btn-outline-dark {{q.qte > 0 ? "" : "disabled"}}">
                  <input type="radio" value="{{ q.taille.id }}" name="taille"><strong>{{ q.taille.libelle }}</strong>
              </label>
              {% if (loop.index)%3 == 0 %}
                </div>
                <div id="btns_taille" class="col-lg-6 col-sm-6 col-6 btn-group btn-group-toggle btns-taille mb-1" data-toggle="buttons">
              {% endif %}
          {% endfor %}
        </div>
      <input type="hidden" value="{{ article.id }}" id="id-article">
      </div>
      <div class="text-right">
        <button class="btn btn-outline-dark btn-rounded add-article-panier disabled">
          <i class="fas fa-shopping-cart mr-2" aria-hidden="true"></i>Ajouter au panier</button>
      </div>
  </div>
</div>
    <!-- Section heading -->
    {#
    <h3 class="mb-5">Détails</h3>

    <div class="row">

        <div class="col-lg-6">
            <img class="img-fluid" src="{{ article.image }}"/>
        </div>

      <div class="col-lg-5 text-center text-md-left">

        <h2 class="h2-responsive text-md-left product-name dark-grey-text mb-1 ml-xl-0 ml-4">
          {{ article.libelle }}
        </h2>
        <h3 class="h3-responsive text-center text-md-left mb-3 ml-xl-0 ml-4">
            <strong>{{ article.prixU }} €</strong>
          </span>
        </h3>
        <div id="description">
         <h3 class="h3-responsive text-center text-md-left mb-2 ml-xl-0 ml-4">Description</h3>
         <p class="text-muted font-weight-light ml-5 mb-5">{{ article.description }}</p>
        </div>

      <section class="taille">

            <p class="text-muted">Choissisez la taille</p>
            <div class="row d-flex flex-wrap div-tailles">
                        <div id="btns_tailles" class="btn-group btn-group-toggle btns-taille" data-toggle="buttons">
                        {% for q in article.getQuantiteTailles %}
                            <label id="btn_taille" class="p-2 btn btn-outline-dark {{q.qte > 0 ? "" : "disabled"}}">
                                <input type="radio" value="{{ q.taille.id }}" name="taille"><strong>{{ q.taille.libelle }}</strong>
                            </label>
                            {% if (loop.index)%3 == 0 %}
                              </div>
                              <div id="btns_tailles" class="btn-group btn-group-toggle btns-taille" data-toggle="buttons">
                            {% endif %}
                        {% endfor %}
                        </div>
                        <input type="hidden" value="{{ article.id }}" id="id-article">
                    </div>
            </div>
          </div>
        </section>
        <div class="row mt-3">
              <div class="col-md-12 text-center">
                <button class="btn btn-outline-dark btn-rounded add-article-panier disabled">
                  <i class="fas fa-shopping-cart mr-2" aria-hidden="true"></i>Ajouter au panier</button>
              </div>
        </div>
    </div>
</section>
</div>
#}
<br>

    {#<div class="item-article">
        <div class="libelle-article">{{ article.libelle }}</div>
        <div class="image-article"><img src="{{ article.image }}"/></div>
        <div class="description-article">{{ article.description }}</div>
        <div class="prix-article">{{ article.prixU }}$</div>
        <select class="custom-select" name="taille" id="taille-article">
            {% for quantiteTaille in article.getQuantiteTailles %}
                {% if quantiteTaille.qte > 0 %}
                    <option value="{{ quantiteTaille.taille.id }}">{{ quantiteTaille.taille.libelle }}</option>
                {% endif %}
            {% endfor %}
        </select>
        <button class="btn btn-cart btn-outline-dark add-article-panier2">Ajouter</button>
        <input type="hidden" value="{{ article.id }}" id="id-article">
    </div>#}

    {#<div class='container-articleSim'>#}
    <div class="presentoir-all">
        {% for articleSimilaire in articlesSimilaire %}
            <div class="card">
              <img src={{ articleSimilaire.image }} class="card-img-top" alt="..." draggable="false">
              <div class="card-body">
                <h5 class="card-title" onclick="{window.location='{{ path("article_show",{id: articleSimilaire.id})}}'}">{{ articleSimilaire.libelle }}</h5>
                <p class="card-text">{{ articleSimilaire.description }}</p>
                {# <a href="#" class="btn btn-primary">Go somewhere</a> #}
              </div>
            </div>
        {#
            <div class='column col-md-2 col-xs-1 m-3' onClick={window.location="{{ path('article_show', {id: articleSimilaire.id}) }}"}>
                <div class="libelle-articleSim">{{ articleSimilaire.libelle }}</div>
                <div class="image-articleSim"><img src="{{ articleSimilaire.image }}"/></div>
                <div class="description-articleSim">{{ articleSimilaire.description }}</div>
                <div class="prix-articleSim">{{ articleSimilaire.prixU }}$</div>
            </div>
        #}
        {% endfor %}
    </div>

{% endblock %}
{% block javascripts %}<script src="/js/article_show.js"></script>{% endblock %}

